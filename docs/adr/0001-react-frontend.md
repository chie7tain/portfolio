# React as the frontend framework

The vanilla JS frontend is being replaced with React as part of the CMS migration. React was chosen over Vue or Svelte because of its larger ecosystem, wider job-market relevance, and better alignment with the backend's TypeScript stack. The migration coincides with the backend build, so there's no cost to switching now.
