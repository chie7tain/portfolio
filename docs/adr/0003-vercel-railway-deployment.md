# Vercel (frontend) + Railway (backend + DB) for deployment

The frontend (React + Vite) deploys to Vercel; the backend (Fastify) and PostgreSQL database run on Railway. Both platforms offer free tiers, zero-config GitHub integration, and automatic deploys on push. A self-hosted VPS was considered but rejected — the operational overhead isn't justified for a personal portfolio project.
