# Drizzle ORM for database access

Drizzle was chosen over raw SQL (too verbose for CRUD operations) and Prisma (too much abstraction, code generation overhead). Drizzle is TypeScript-native, lightweight, and uses SQL-like syntax — meaning it teaches PostgreSQL concepts rather than hiding them, which matters given this is also a learning project.
