# Node.js + Fastify as the backend framework

Fastify was chosen over Express (too minimal, no structure) and NestJS (too much boilerplate for a solo project). Fastify provides TypeScript-first support, a plugin system, and strong PostgreSQL integration, making it a well-balanced fit for a content API at this scale.
