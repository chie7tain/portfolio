# Tailwind CSS for styling

Tailwind was chosen over plain CSS (migrating the existing hand-written styles) and CSS Modules. It pairs naturally with React's component model, eliminates CSS naming overhead, and the existing design tokens (--mainRed, --mainBlack, etc.) translate directly into a Tailwind theme config. CSS Modules were rejected as an unnecessary middle ground.
