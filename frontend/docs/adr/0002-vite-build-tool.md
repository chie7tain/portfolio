# Vite as the build tool

Vite was chosen over Create React App, which is largely unmaintained and significantly slower. Vite is the current community standard for new React projects — fast HMR, minimal config, and first-class TypeScript support out of the box.
